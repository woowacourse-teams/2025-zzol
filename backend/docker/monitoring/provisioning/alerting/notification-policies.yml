# Grafana Notification Policies - 알림 라우팅 설정
apiVersion: 1

policies:
  - orgId: 1
    receiver: grafana-default-email
    group_by:
      - alertname
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
