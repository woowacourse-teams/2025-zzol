# 이 파일은 의도적으로 비어 있습니다.
#
# nginx 기본 이미지의 /etc/nginx/conf.d/ 디렉토리에 마운트되므로
# http {} 블록 안에 include됩니다.
# main context 지시자(user, worker_processes, events {}, http {})는
# 이 위치에서 사용할 수 없어 nginx 시작 오류를 유발합니다.
#
# http 레벨 공통 설정은 00-common.conf 참조.
