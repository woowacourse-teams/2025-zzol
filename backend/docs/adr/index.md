# ADR 인덱스

주요 기술 의사결정 목록. 새 ADR 작성은 `/adr [주제]`로 한다.

코드 작성 시 **영향 범위**와 작업 중인 패키지/주제를 비교한다. 겹치는 항목이 있으면 해당 ADR 파일을 읽어 핵심 제약과 충돌 여부를 확인한다.

| 번호                                                            | 제목                                                                            | 상태                    | 영향 범위                                                                                                                                      | 핵심 제약                                                                                                                            |
|---------------------------------------------------------------|-------------------------------------------------------------------------------|-----------------------|--------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------|
| [0001](0001-ai-nickname-audit-with-operator-feedback-loop.md) | AI 닉네임 검열 + 운영자 피드백 루프 설계                                                     | 승인 (후속 작업 예정)         | `room/infra/nickname`, `room/application/service/nickname`                                                                                 | FLAGGED(confidence ≥ 0.85)는 자동 차단 후 어드민 해제 가능, PENDING(< 0.85)은 운영자 수동 결정                                                        |
| [0002](0002-block-stacking-game-design.md)                    | BlockStacking 게임 서버 설계                                                        | 승인                    | `blockstacking/`, `global/flow/`, `minigame/domain/MiniGameType`                                                                           | 잘못된 progress 이벤트는 무시+warn 로그만, 20초 서버 타이머가 단일 종료 기준                                                                              |
| [0003](0003-ladder-game-design.md)                            | LadderGame 게임 서버 설계                                                           | 승인                    | `laddergame/`, `minigame/domain/MiniGameType`                                                                                              | row는 서버 수신 순서로 결정, 인접 구간 동일 row 충돌 시 아래로 밀어 해소, DRAWING은 항상 5초                                                                   |
| [0004](0004-oauth2-login-and-user-domain.md)                  | OAuth2 로그인 도입과 User 도메인 분리                                                    | 승인                    | `user/`, `room/infra/persistence/`, `auth/`                                                                                                | Player-User는 nullable FK로 분리, 회원 입장 시 프로필 닉네임 강제, 익명 흐름 그대로 유지                                                                   |
| [0005](0005-jwt-refresh-token-policy.md)                      | Access(JWT) + Refresh(Redis) 인증 토큰 정책                                         | 승인                    | `user/config/`, `user/application/service/`, `user/infra/redis/`                                                                           | Refresh token은 `{userId}:{tokenId}` 형식, 재사용 감지 시 family 전체 폐기                                                                    |
| [0006](0006-user-code-policy.md)                              | 5자리 사용자 식별 코드(UserCode) 정책                                                    | 승인                    | `user/domain/`, `user/domain/service/`                                                                                                     | UserCode는 가입 시 1회 발급 후 불변, INSERT-then-catch + 최대 100회 재시도                                                                       |
| [0007](0007-oauth-token-delivery-via-one-time-code.md)        | OAuth 로그인 토큰 전달 — 일회용 코드 교환 방식                                                | 승인                    | `user/ui/`, `user/application/service/`, `user/infra/redis/`                                                                               | OAuth 성공 후 토큰은 URL에 직접 노출 금지, 반드시 `/auth/token` 코드 교환을 경유해야 함                                                                    |
| [0008](0008-roulette-adjustment-weight-http-configuration.md) | 룰렛 확률 조정 가중치(ADJUSTMENT_WEIGHT) HTTP 설정 가능화                                   | 승인                    | `room/domain/roulette/`, `room/domain/`, `room/config/`, `room/ui/`                                                                        | 가중치는 READY 상태에서만 변경 가능, 유효 범위 0.1 이상 0.9 이하                                                                                      |
| [0009](0009-websocket-room-session-token-auth.md)             | WebSocket 인증 — Room Session Token 도입                                          | 승인 (2026-05-15 부분 개정) | `global/websocket/interceptor/`, `room/ui/`, `room/application/`                                                                           | 방 연결: `roomToken`(RST) 필수 → `PlayerKey` Principal / 사용자 연결: `Authorization` Bearer → `UserPrincipal` / 익명: `sessionId` 폴백        |
| [0010](0010-stomp-session-manager-redis.md)                   | StompSessionManager — 인메모리에서 Redis 기반으로 전환                                    | 보류                    | `global/websocket/`, `global/websocket/infra/`, `global/websocket/event/`                                                                  | 세션 조회·등록은 반드시 Redis 경유, 로컬 인메모리 맵 사용 금지                                                                                          |
| [0011](0011-multi-module-migration.md)                        | Gradle 멀티 모듈 전환                                                               | 적용됨                   | 전체 패키지, `build.gradle.kts`, `settings.gradle.kts`                                                                                          | 10모듈: `:common/:infra/:websocket/:game-api/:user/:room/:game/:admin/:zzolbot/:app`; `Playable/MiniGameFactory` → `:game-api`; `PlayerView` → `Gamer` record |
| [0012](0012-websocket-catalog-discovery.md)                   | WebSocket 컨트랙트 디스커버리 — `@WsTopic`/`@WsQueue`/`@WsReceive` + `/dev/ws-catalog` | 보류                    | `global/websocket/docs/`, 도메인별 컨트롤러/Publisher/Notifier (총 15개), `friend/application/FriendNotifier`, `friend/application/PresenceNotifier` | 토픽 발행은 `@WsTopic`, 개인 큐 발행은 `@WsQueue`, 비동기 핸들러는 `@WsReceive` 사용. `Object.class` payload 금지. 동일 path 다중 발행자는 `publishers` 배열로 묶임 |
| [0013](0013-domain-module-test-isolation.md)                  | 도메인 모듈 테스트 독립 실행 전략                                                           | 적용됨                   | `*/src/test/`, `:test-support`, `build.gradle.kts` (testcontainers 버전)                                                                      | TestApplication + TestConfig 패턴; `:app` 고유 빈은 모듈별 TestConfig에서 mock; 공통 설정은 `application-test-base.yml`에 집중; `CommonTestSchedulerConfig`로 공통 스케줄러 빈 중앙화; `IntegrationTestSupport`에 `@BeforeEach` DB cleanup 포함; 모듈별 독립 DB·Redis 인덱스 격리로 CI에서도 Testcontainers reuse 활성화 |
| [0014](0014-shared-web-module.md)                             | 공유 웹 인프라 모듈 `:web` 도입                                                          | 적용됨                   | `RestExceptionHandler`, `SwaggerConfig`, 전 도메인 모듈 `build.gradle.kts`, 루트 `build.gradle.kts`                                               | `:web`은 `:common` 위에 위치; REST 엔드포인트 모듈은 `:web` 의존; `web`, `validation`, `springdoc`은 `:web`이 `api`로 노출 |
| [0015](0015-test-base-class-inheritance.md)                   | `:test-support` 베이스 테스트 클래스 계층 설계                                               | 적용됨                   | `*/src/test/`, `:test-support`, `ServiceTest`, `IntegrationTestSupport`                                                                     | `@SpringBootTest`는 실행 모듈 클래스패스 기준; Spring `MergedAnnotations`로 상위 클래스 `@Import` 합산; 도메인 모듈은 `coffeeshout.support.ServiceTest` 직접 상속 |
| [0016](0016-test-fixture-structure.md)                        | 테스트 픽스처 구조 — testFixtures 소스 셋과 유형별 네이밍 전략                                      | 적용됨                   | `*/src/testFixtures/`, `*/src/test/java/coffeeshout/fixture/`, `build.gradle.kts`                                                           | 공유 픽스처는 `src/testFixtures/coffeeshout.fixture`; `*Fixture`(팩토리) / `*Fake` / `*Dummy` / `Stub*` / `TestDataHelper`(@Component DB헬퍼) 유형 구분 |
| [0017](0017-integration-test-web-environment.md) | `IntegrationTestSupport` WebEnvironment — `MOCK` 기본, `RANDOM_PORT` 명시 오버라이드 | 적용됨 | `*/src/test/`, `:test-support`, `app/IntegrationTestSupport`, `user/UserModuleIntegrationTest` | `MOCK`이 기본; WebSocket·`TestRestTemplate` 사용 시만 `RANDOM_PORT` 명시 오버라이드 |
| [0018](0018-profanity-module-extraction.md)                   | 비속어 필터링 자체 모듈 전환 — `:profanity`                                               | 승인                    | `:profanity`(신규), `room/infra/`, `room/domain/service/ProfanityChecker`, `room/application/service/player/name/`, `admin/`                   | `ProfanityWordBlockedEvent`는 `:common` 유지; 다중 인스턴스 동기화는 Redis pub/sub; 단어 진실 공급원은 `profanity_word` 테이블로 일원화; AI 레이어는 `:profanity-ai`로 분리 예정 |
| [0019](0019-nickname-audit-domain-jpa-entity.md)              | NicknameAudit / NicknameFeedback — JPA 어노테이션을 가진 도메인 객체                        | 승인                    | `profanity/domain/audit/`, `profanity/application/`, `profanity/infra/persistence/audit/`, `admin/`, `room/infra/`                             | domain 레이어가 `jakarta.persistence`에 의존하는 것을 허용; 별도 매핑 객체 도입 금지; JPA Repository는 `infra.persistence.audit`에 잔류 |
| [0020](0020-spring-boot-4-migration.md)                       | Spring Boot 4 마이그레이션 — 3단계 점진 전환                                              | 제안                    | 전 모듈 `build.gradle.kts`, `gradle/libs.versions.toml`, QueryDSL 사용 6개 모듈, WebSocket 컨트랙트 직렬화                                          | Phase 1(QueryDSL fork 교체) → 2(Boot 4 + Jackson 2 호환) → 3(Jackson 3 네이티브) 순서 준수; `@CircuitBreaker`/`@RateLimiter`는 resilience4j 유지 |
| [0021](0021-trace-propagation-traceparent.md)                 | Redis Stream 트레이스 전파 — TraceInfo embed → W3C traceparent 캐리어                   | 승인                    | `infra/global/redis/`, `infra/global/outbox/`, `:room`·`:game` 이벤트 19개, `game/*/config/` 스케줄러 6개                                        | 전파는 인프라 경계 2곳(`StreamPublisher` inject / Listener extract)에서만; 도메인 이벤트는 트레이싱 무지; Outbox는 기록 시점에 traceparent 저장; 컨슈머 측 `sampled(true)` 강제 금지; 전파 검증 테스트엔 `@AutoConfigureObservability` 필수 |
| [0022](0022-redis-stream-listener-start-offset.md)            | Redis Stream 리스너 시작 오프셋 — fromStart 리플레이 제거와 구독 활성화 대기                        | 승인                    | `infra/global/redis/config/`, `infra/global/health/`, `infra/global/metric/`, `test-support/application-test-base.yml`, `app/config/redis.yml`                | 시작 오프셋은 기동 시점 마지막 ID(XREVRANGE) 해석 — `latest($)` 사용 금지(매 폴링 `$` 재사용으로 상시 유실); 구독 등록은 register→start→await 순서, await 실패 시 기동 실패; errorHandler는 `buildReadRequest()` 단일 경유; 공유 풀 core-size ≥ 사용 스트림 수(폴링 태스크가 스레드 영구 점유) |
| [0023](0023-gitops-edge-config-reconciliation.md)             | GitOps 엣지 설정 동기화 — nginx/monitoring reconciliation과 edge-cd 분리              | 제안                    | `.github/workflows/edge-cd.yml`(신규), `.github/scripts/`, `backend/docker/nginx/`, `backend/docker/monitoring/`, `.gitattributes`                | edge-cd는 `*-service.inc`(라이브 Blue/Green) 동기화 금지(`rsync --exclude`); conf는 best 병합·compose는 서버 현실 채택; 적용은 백업→sync→`nginx -t`→reload→(필요 시)`docker compose up -d`→실제 경로 프로브→실패 시 원복; 워크플로우는 main+be/dev 양쪽; nginx conf는 LF 고정 |
| [0024](0024-user-identity-unification.md)                     | 사용자 식별 통합 — 중복 닉네임 허용과 `name#UserCode` 전역 식별자                                 | 제안 (미결 질문)            | `game-api/gamecommon/Gamer`, `room/domain/player`, WebSocket 컨트랙트, 친구·프로필 도메인, ADR-0025 `GameSession` 식별                                      | 전역 식별은 `name#UserCode`(`userId` 외부 노출 금지); 식별자·타입을 `GamerId{type, key}`로 분리(non-null 키); 게스트 키는 `PlayerKey` 재사용(A) 또는 이름 유니크 스톱갭(C) — Q1 미결; 중복 닉네임 차단 해제는 회원부터                                                                                                                            |
| [0025](0025-room-gamesession-separation.md)                   | Room-GameSession 분리 — 게임 대기열 소유권을 `:game`으로 이관                                | 승인 (구현 완료)            | `room/domain/Room.java`, `room/ui/RoomRestController`, `game/minigame/*`, `game-api/gamecommon/`, `infra/EventDispatcher`                      | `GameSession`이 게임 대기열·완료 목록·상태(`READY/PLAYING/DONE`) 소유, 대기열 변경은 READY+호스트 검증 필수; `JoinCode`는 `:game-api` 순수 값 객체(QrCode 분리); 게임 결과는 `MiniGameFinishedEvent` in-process 동기 리스너 경유(`@Async` 금지); `roundCount`(선택 게임 총수)는 GameSession 소유; `:room`은 게임 타입, `:game`은 `Player` import 금지                            |
| [0026](0026-alertmanager-and-alerting-rules.md)               | Alertmanager 단일 알림 엔진 채택 — Grafana Alerting 통합과 2단계 롤아웃                   | 제안                    | `backend/docker/monitoring/conf/prometheus.yml`, `conf/alertmanager.yml`(신규), `conf/rules/*.yml`(신규), `docker-compose.yml`, `provisioning/alerting/`, `IpBlockFilter`(후속) | Grafana Alerting(룰 8개 존재·contactPoints 비어 외부 전달 0)을 Alertmanager로 통합·일원화; 빅뱅 금지·**2단계 롤아웃**(Phase A #1399: Alertmanager+Slack+신규 인시던트룰/SLO + 기존 Grafana에 Slack contact point 추가로 즉시 전달 / Phase B 별도 이슈: 8룰 Prometheus 이관 후 Grafana Alerting 제거) — 어느 시점에도 커버리지 공백 없음; **이관 제약: `noDataState:Alerting`(JVM힙·WS지연·서킷브레이커·CPU)은 Prometheus 등가물 없어 `absent()` 동반룰 필수**(미적용 시 silent 알림 중단); 시크릿은 `slack_api_url_file`(env 확장 안 함); 임계값 추정치라 베이스라인 후 튜닝; 배포는 edge-cd(ADR-0023) 위, 서버 직접 수정 금지 |
| [0027](0027-promtail-to-alloy-migration.md)                   | 로그 콜렉터 Promtail → Grafana Alloy 마이그레이션                                    | 승인                    | `backend/docker/dev/conf/config.alloy`(신규), `backend/docker/prod/conf/config.alloy`(신규), `backend/docker/{dev,prod}/docker-compose.yml`, monitoring 로그 파이프라인(Loki 인입) | Promtail EOL(2026-03-02)로 Alloy(OTel Collector 기반 후속)로 교체; `alloy convert`로 promtail.yml→River 변환 후 사람이 검수·`alloy validate`로 게이트(convert가 낸 `level=null`→`level=""` 수정); 이미지 `grafana/alloy:v1.16.3` 버전 고정으로 prod `latest` 안티패턴 동시 해소; **dev/prod 타임스탬프 차이 보존**(dev 미변환·ingestion 시각 / prod KST→UTC `Asia/Seoul`); healthcheck 미추가(이미지에 wget/curl 없음); 배포는 edge-cd 아닌 **backend-cd** 경로 — 서비스 리네임(`*-promtail`→`*-alloy`)에 따른 고아 컨테이너 정리 배포 시 확인; **머지·배포 체크리스트 포함** |
| [0029](0029-api-mcp-http-openapi-expansion.md)                | api-mcp — WebSocket 디스커버리에서 HTTP/OpenAPI 디스커버리로 확장 + `ws-mcp` 리네임            | 제안 (구현 완료, PR #1289) | `tools/api-mcp/`, `backend/.mcp.json`, `.github/workflows/api-mcp-ci.yml`, `.github/dependabot.yml` | ADR-0012(WS 디스커버리) 후속; `ws-mcp`→`api-mcp` 일괄 리네임(디렉터리·패키지·서버키·CI·dependabot); springdoc `/v3/api-docs` 소비하는 `http_*` 도구 4종 추가(list_endpoints·describe·request·validate, `http_request`는 body 금지 메서드 가드); env 접두사 `API_MCP_*`로 통일(`WS_MCP_*`/`HTTP_MCP_BASE_URL` 폐기, breaking — 셸 override 갱신 필요); `http_*`·STOMP 검증은 BE 필요, `ws_*` 디스커버리는 캐시로 오프라인 가능; `http_request`는 로컬/개발 서버 대상 개발 도구 전제 |
| [0030](0030-alloy-trace-metric-collection-integration.md)     | 트레이스·메트릭 수집의 Alloy 통합 검토 — 콜렉터 일원화와 백엔드 불변                                | 제안 (초안 — 결정 보류)       | `app/.../config/monitoring.yml`·`config/zzolbot.yml`(현황), `backend/docker/{dev,prod}/conf/config.alloy`·`monitoring/conf/prometheus.yml`(통합 시 변경 지점) | **Alloy는 콜렉터(agent)이지 저장·질의 백엔드가 아님** — "트레이스 Alloy 통합"=Tempo 앞 OTLP 게이트웨이 삽입이지 Tempo 대체 아님; **"Tempo 없이 일관화" 불가**(zzol-bot이 `tempo:3200` 질의 의존·Grafana 데이터소스 의존, Alloy는 TraceQL 저장·질의 불가); 일원화는 *수집 계층*에 한정(LGTM 토폴로지, 백엔드 3개 불변); **현 시점 통합 보류(defer)**·신호별 분리 판단(트레이스 게이트웨이=방어가능하나 현 규모 직결로 충분 / 메트릭=churn·`up{job}` 의미 손상으로 Prometheus pull 유지); 통합 시 직결 경로 제거로 이중수집 방지 필수; ADR-0028 결합 시 blast radius가 로그→로그+트레이스로 확대; 재검토 트리거=tail-sampling·Tempo장애 버퍼링·다중송신원 |
