package coffeeshout.cardgame.proof;

import static org.junit.jupiter.api.Assertions.fail;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

/**
 * 카드게임 규칙 증명 테스트 — 구현자가 채우는 스켈레톤.
 * <p>
 * 각 메서드명이 곧 증명해야 할 성질이다. 본문은 구현 아키텍처에 맞게 자유롭게 작성한다(순수 단위 테스트든
 * 컨텍스트를 올리는 테스트든, 어떤 픽스처·베이스 클래스를 쓰든 무방하다). 단 아래는 지켜야 한다.
 * <ul>
 *   <li>메서드를 삭제·개명하거나 시그니처·어노테이션을 바꾸지 않는다. {@code @Disabled}를 추가하지 않는다.</li>
 *   <li>8건 전부를 채워야 빌드 게이트를 통과한다. {@code fail("TODO ...")}가 남아 있으면 통과하지 못한다.</li>
 *   <li>본문에는 검증(assertion 또는 verify)이 최소 1개 있어야 하며, 메서드명이 주장하는 성질을 실제로 검증해야 한다.</li>
 * </ul>
 */
@DisplayName("카드게임 규칙 증명")
class CardGameRuleProofTest {

    @Test
    @DisplayName("동시에 도착한 두 선택 명령 중 정확히 하나만 상태를 전이시킨다")
    void 동시에_도착한_두_선택_명령_중_정확히_하나만_상태를_전이시킨다() {
        fail("TODO: 같은 카드(또는 같은 플레이어의 두 카드)를 두 실행 경로가 동시에 선택할 때 한쪽만 성공함을 증명한다");
    }

    @Test
    @DisplayName("상태 전이가 실패하면 알림이 발행되지 않는다")
    void 상태_전이가_실패하면_알림이_발행되지_않는다() {
        fail("TODO: 전이가 거부·실패했을 때 어떤 알림도 나가지 않음을 증명한다");
    }

    @Test
    @DisplayName("알림은 상태 전이가 확정된 뒤에만 발행된다")
    void 알림은_상태_전이가_확정된_뒤에만_발행된다() {
        fail("TODO: 알림 발행 시점에 전이가 이미 확정되어 있음(순서)을 증명한다");
    }

    @Test
    @DisplayName("같은 선택 명령이 두 번 도착해도 상태는 한 번만 전이된다")
    void 같은_선택_명령이_두_번_도착해도_상태는_한_번만_전이된다() {
        fail("TODO: 동일 명령의 중복 도착(재전송·재시도)이 이중 전이를 만들지 않음을 증명한다");
    }

    @Test
    @DisplayName("알림 페이로드는 전이 시점 객체가 아니라 확정된 최신 상태다")
    void 알림_페이로드는_전이_시점_객체가_아니라_확정된_최신_상태다() {
        fail("TODO: 발행되는 상태 페이로드가 확정된 최신 전체 스냅샷임을 증명한다");
    }

    @Test
    @DisplayName("재구독 요청에 현재 상태를 다시 발행한다")
    void 재구독_요청에_현재_상태를_다시_발행한다() {
        fail("TODO: 재연결·재구독한 클라이언트가 현재 상태 스냅샷을 받을 수 있음을 증명한다");
    }

    @Test
    @DisplayName("같은 페이즈 마감은 두 번 발화되지 않는다")
    void 같은_페이즈_마감은_두_번_발화되지_않는다() {
        fail("TODO: 하나의 페이즈 마감이 중복 발화(이중 전이)되지 않음을 증명한다");
    }

    @Test
    @DisplayName("서로 다른 경로로 도착한 명령이 하나의 게임 상태로 수렴한다")
    void 서로_다른_경로로_도착한_명령이_하나의_게임_상태로_수렴한다() {
        fail("TODO: 같은 게임의 명령이 서로 다른 인스턴스(경로)에 도착해도 하나의 일관된 상태가 됨을 증명한다");
    }
}
