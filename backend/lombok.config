# Lombok이 생성하는 생성자(@RequiredArgsConstructor 등)에 필드 레벨 @Qualifier를 복사한다.
# 기본값에선 필드의 @Qualifier가 생성자 파라미터로 전달되지 않아(주입 시 무시됨),
# 같은 타입 빈이 둘 이상이 되면 엉뚱한 빈이 주입될 수 있다. 이를 전역으로 방지한다.
config.stopBubbling = true
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
